version:v2
modules:
    - path: .
      name: codemarie/proto/lint

lint:
    use:
        - STANDARD

    except:
        - RPC_PASCAL_CASE
        - RPC_REQUEST_RESPONSE_UNIQUE
        - RPC_REQUEST_STANDARD_NAME
        - RPC_RESPONSE_STANDARD_NAME
        - PACKAGE_VERSION_SUFFIX
        - ENUM_VALUE_PREFIX
        - ENUM_ZERO_VALUE_SUFFIX
